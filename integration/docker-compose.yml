version: '3.1'

services:
  generator:
    image: internal-registry:5000/generator:${GENERATOR_BUILD_TAG-10}
    #ports:
    #  - "8080:80"
  mail:
    image: mailhog/mailhog
    #ports:
    #  - "8025:8025"
  integration:
    image: internal-registry:5000/generator:${INTEGRATION_BUILD_TAG-10}
    build: .
    environment:
      - TEAMCITY_PROJECT_NAME